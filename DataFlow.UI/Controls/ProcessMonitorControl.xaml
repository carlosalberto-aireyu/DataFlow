<UserControl x:Class="DataFlow.UI.Controls.ProcessMonitorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataFlow.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <UserControl.Resources>
        <Style x:Key="InfoNotificationStyle" TargetType="Border">
            <Setter Property="Background" Value="#E8F5E9"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="1,0,0,0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="WarningNotificationStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF8E1"/>
            <Setter Property="BorderBrush" Value="#FFC107"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="ErrorNotificationStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFEBEE"/>
            <Setter Property="BorderBrush" Value="#F44336"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0" Background="#F5F5F5" Padding="10,8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋 Log de Procesamiento" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusTextBlock" Text="Listo" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="#666"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Limpiar" Click="ClearButton_Click" 
                        Padding="12,4" Background="White" BorderBrush="#BDBDBD"/>
            </Grid>
        </Border>


        <ScrollViewer Grid.Row="1" x:Name="NotificationScrollViewer" 
                     VerticalScrollBarVisibility="Auto"
                     Background="White">
            <ItemsControl x:Name="NotificationsItemsControl" Margin="5">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="NotificationBorder">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>


                                <TextBlock Grid.Column="0" 
                                          x:Name="IconBlock"
                                          FontSize="16" 
                                          Margin="0,0,8,0" 
                                          VerticalAlignment="Top"/>


                                <StackPanel Grid.Column="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" 
                                                  Text="{Binding Message}" 
                                                  TextWrapping="Wrap"
                                                  FontSize="13"/>

                                        <TextBlock Grid.Column="1" 
                                                  Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" 
                                                  FontSize="11" 
                                                  Foreground="#999"
                                                  Margin="8,0,0,0"/>
                                    </Grid>


                                    <TextBlock Text="{Binding Details}" 
                                              TextWrapping="Wrap"
                                              FontSize="11" 
                                              Foreground="#666"
                                              Margin="0,2,0,0"
                                              Visibility="{Binding HasDetails, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Level}" Value="Info">
                                <Setter TargetName="NotificationBorder" Property="Style" Value="{StaticResource InfoNotificationStyle}"/>
                                <Setter TargetName="IconBlock" Property="Text" Value="ℹ️"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                <Setter TargetName="NotificationBorder" Property="Style" Value="{StaticResource WarningNotificationStyle}"/>
                                <Setter TargetName="IconBlock" Property="Text" Value="⚠️"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                <Setter TargetName="NotificationBorder" Property="Style" Value="{StaticResource ErrorNotificationStyle}"/>
                                <Setter TargetName="IconBlock" Property="Text" Value="❌"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#FAFAFA" Padding="10,8" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock x:Name="InfoCountTextBlock" Text="0" FontWeight="SemiBold"/>
                    <TextBlock Text=" Info" Margin="3,0,0,0" Foreground="#666"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="⚠️" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock x:Name="WarningCountTextBlock" Text="0" FontWeight="SemiBold"/>
                    <TextBlock Text=" Advertencias" Margin="3,0,0,0" Foreground="#666"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="❌" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock x:Name="ErrorCountTextBlock" Text="0" FontWeight="SemiBold"/>
                    <TextBlock Text=" Errores" Margin="3,0,0,0" Foreground="#666"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
