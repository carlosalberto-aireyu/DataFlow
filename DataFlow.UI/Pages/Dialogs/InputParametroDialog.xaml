<Window x:Class="DataFlow.UI.Pages.Dialogs.InputParametroDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Detalles del Parámetro" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"><Run Text="Opción"/><Run Language="es-bo" Text=":"/></TextBlock>
        <TextBox 
            x:Name="NameTextBox" 
            Grid.Row="0" 
            Grid.Column="1" 
            Text="{Binding ParameterName, UpdateSourceTrigger=PropertyChanged}" 
            Margin="0,5,0,5" 
            IsEnabled="{Binding !DisableKeyEdit}"
            Style="{StaticResource ReadOnlyTextBoxStyle}"
            />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Valor:" VerticalAlignment="Center" Margin="0,5,10,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding ParametroValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5,0,5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Descripción:" VerticalAlignment="Center" Margin="0,5,10,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5,0,5" 
                 TextWrapping="Wrap" Height="60"
                 IsEnabled="{Binding !DisableKeyEdit}"
                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                 />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Guardar" Click="SaveButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Click="CancelButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
